# 装配式桥梁 UHPC 湿接缝施工荷载放行质量闭环

## 项目目标

建设一个可编译运行、可重启恢复的 Go HTTP 后端，管理装配式桥梁预制梁板 UHPC 湿接缝的设计锁定、材料领用、设备租约、拌合浇筑、养护检测、异常复验、双人复核与终局裁定。核心模型为“接缝—纵向分段—浇筑层”的只追加填充格，以及按锁定流向推进的连续浇筑前缀；所有质量计算使用带溢出检查的整数定点算法，所有状态变更以事务事件和当前快照持久化。实现规模限定为约 20—30 个生产 Go 文件、2000—3000 行有效生产代码并以约 2500 行为目标，划分为至少 4 个有业务意义的包；公开测试不少于 4 个测试文件和 12 个独立用例。仓库包含 go.mod、启动配置、数据库迁移和支持 linux/amd64、linux/arm64 构建的 Dockerfile。

## 端到端业务流程

1. 设计锁定：录入桥跨、接缝编号、整数毫米槽口范围、纵向分段、浇筑层、钢筋保护层与搭接区、剪力键、基面粗糙度测区、预湿要求、UHPC 配方修订、材料批次、盘次计划、流向、养护日程、同条件试件、拉拔点、收缩测点和阈值；校验桥跨与配方匹配、连续无交叠覆盖、钢筋与剪力键无冲突及规则摘要最新后，生成不可变锁定摘要。
2. 基面交接：按测区追加粗糙度、清洁度和预湿证据；只有全部锁定测区满足要求并经确认，相关首段才可进入拌合准备，仪器失败只登记确定次数的待重试调用。
3. 材料与设备准备：施工方以操作号申请某盘材料和所需设备；事务内从粉料、水、外加剂、钢纤维守恒池扣减整数克，同时取得拌合机、称量台等限时互斥租约。称量允许值、试拌取样和合理损耗均进入质量账，任何冲突或偏差使整个事务回滚。
4. 拌合与流动度：依锁定盘次记录各组分实投量、纤维投放和逻辑时间；成功盘次生成不可合并的材料代次及确定施工截止时刻。流动度采集成功且定点判定合格后才可浇筑；异常、超时或格式错误不得生成可施工授权。
5. 连续浇筑与养护：持有效设备租约及未过期材料代次，按锁定流向逐格追加浇筑层和分段振实证据。每次写入必须把连续前缀恰好推进一格；随后按日程追加养护毯回路、温湿度和保湿时长证据，闭合养护时间轴。
6. 检测与复验：按逻辑时间录入同条件试件抗压强度、界面拉拔、收缩和裂缝/外观证据。任一异常依据锁定邻接表、同盘材料代次和共享基面测区求闭包，生成唯一有序复验集合；处置只能建立新代次重新覆盖受影响分段、试件和测点，原始谱系和证据永久保留。
7. 复核与终局：在材料守恒、全部填充前缀完成、养护闭合、几何外观合格、强度与粘结达标且复验完成后，由两名不同且具资格的人员独立提交相同结论。仲裁器通过单写终局屏障在允许通车、质量隔离、取消之间只接受一个终局，并为允许通车生成唯一凭据。
8. 重启恢复：服务启动时读取持久化快照并校验事件序列，恢复材料余额、活跃代次、填充前缀、待重试调用、未过期租约和终局；过期租约按逻辑时钟惰性失效，已提交操作号继续保持幂等。

## 核心组件与职责

1. 接缝设计与 UHPC 材料规则目录：管理桥跨、接缝设计、几何区间、钢筋与剪力键约束、测区、配方修订、阈值、锁定摘要和整数定点规则。
2. 湿接缝任务及覆盖聚合：维护只追加填充格、锁定流向、连续前缀、施工依赖、养护时间轴及接缝当前代次视图。
3. 材料守恒与设备租约管理器：维护整数克批次账、取样与损耗账、原子领料、限时互斥设备租约、幂等操作记录和事务回滚。
4. 拌合浇筑及养护证据记录器：记录不可合并盘次代次、施工期限、流动度、浇筑振实、养护样点、设备调用失败及确定性重试状态。
5. 强度粘结复验与终局仲裁器：处理强度、拉拔、收缩、裂缝和外观判定，计算唯一复验闭包，隔离迟到旧代次证据，执行双人复核及单写终局。
6. Go HTTP API：提供版本化 JSON 接口、稳定错误码、请求校验、事务边界、确定排序和健康/恢复状态查询。

## 领域规则与不变量

1. 锁定后，桥跨、接缝编号、槽口几何、分段与层、钢筋搭接、剪力键、基面测区、配方、材料批次、盘次计划、流向、养护日程、试件、测点、邻接表和阈值均不可原位修改；处置只能产生引用旧代次的新代次。
2. 设计范围和分段坐标使用整数毫米。分段必须按起点、终点和编号稳定排序后连续覆盖设计范围，不得缺失、交叠或退化；保护层、搭接区及剪力键校验拒绝负数，并对所有加法、乘法和尺度换算检查有符号 64 位溢出。
3. 粉料、水、外加剂和钢纤维均以整数克记账；初始入池质量必须等于可用余额、各盘实投、试拌取样、批准损耗和隔离余料之和。材料扣减与设备租约获取共用一个事务，任一步失败不得遗留扣减或租约。
4. 拌合机、称量台、浇筑设备、养护毯回路、压力机和拉拔仪按资源标识互斥租用。租约含持有人、用途、开始逻辑时间和截止时间；只有持有人可续租或释放，截止边界时视为过期，失效租约不能支持状态推进。
5. 每盘合格拌合产生唯一材料代次，不能与其他盘合并。施工期限按锁定规则由拌合完成逻辑时间确定；截止时刻前可写入，等于或晚于截止时刻拒绝。流动度异常、称量偏差或纤维不守恒的盘次永远不能写入有效接缝。
6. 基面确认、拌合、流动度、浇筑、振实、养护和检测严格按锁定依赖推进。填充格只可追加一次，写入盘次必须与计划相符，并且只能推进下一个连续前缀；跳段、重复层、逻辑时间倒退或过期材料均返回稳定错误。
7. 水胶比、纤维掺量、流动度、浇筑间隔、养护温湿度时长、收缩率、抗压强度和界面粘结强度统一采用明确尺度的整数定点值；规则指定半远离零舍入，拒绝除零、非法符号、尺度错配和中间乘加溢出。
8. 仪器拒绝、断连、超时或格式错误只生成包含调用键、逻辑时间、故障类别、下一重试序号和固定最大次数的待重试记录；不得伪造读数或推进业务状态。相同调用键的成功回执只能消费一次。
9. 复验集合为异常分段、其锁定相邻分段、同盘材料覆盖分段及共享基面测区关联分段的传递闭包，并按桥跨、接缝、纵向分段、浇筑层、盘次、测点和代次排序。相同异常事实只能生成一个集合；新代次激活后，旧代次迟到回执仅归档，不改变当前结论。
10. 操作号绑定请求内容摘要和已提交响应；同号同内容重试返回原结果，同号不同内容返回 IDEMPOTENCY_CONFLICT。允许通车、质量隔离和取消共享单写终局屏障，并发提交只允许一个成功。所有拒绝返回稳定错误码和有序原因，失败事务不得留下材料扣减、设备租约、填充前缀、有效养护证据或通车凭据。

## 数据模型与持久化

1. BridgeSpan：桥跨标识、工程坐标尺度、允许配方集合、当前规则摘要。JointDesign：接缝编号、槽口起止、分段、浇筑层、流向、钢筋区间、剪力键位置、基面测区、邻接表及锁定版本。
2. UHPCRecipeRevision：修订号、粉料/水/外加剂/纤维目标克数、允许偏差、水胶比和纤维掺量尺度、流动度、施工期限、养护、收缩、强度及粘结阈值。
3. MaterialBatch 与 MaterialLedgerEntry：材料类别、供应批次、初始克数、扣减、取样、损耗、隔离和余额；MixPlan、MixRun 与 MaterialGeneration 保存盘次顺序、实投谱系、拌合逻辑时间、施工截止时间和有效性。
4. EquipmentLease：设备类别、资源标识、持有人、用途、租约令牌、起止逻辑时间和状态。DeviceCall：调用键、仪器、请求摘要、尝试序号、故障类别、读数、逻辑时间和待重试/成功状态。
5. JointTask、FillCell 与 PourPrefix：锁定摘要、当前代次、分段—层坐标、计划盘次、只追加填充证据、振实证据及连续前缀位置。SurfaceEvidence 与 CuringEvidence：测区处理、预湿、温湿度样点、养护毯回路和闭合区间。
6. Specimen、PullOffPoint、ShrinkagePoint 与 InspectionEvidence：试件/测点标识、所属分段和材料代次、整数定点读数、仪器调用、判定及裂缝外观证据。
7. Anomaly、RetestSet 与 RemediationGeneration：异常类型、来源证据、确定性集合摘要、有序影响范围、新旧代次关系、重新覆盖要求和完成状态。
8. Review 与 FinalVerdict：人员标识、资格快照、独立复核结论、内容摘要、终局类型、屏障版本和唯一通车凭据。OperationRecord 与 DomainEvent：操作号、请求摘要、响应、提交序号和用于重启恢复的事务事件。

## 公开接口

1. POST /v1/joints 与 POST /v1/joints/{id}/lock：创建并锁定接缝任务；返回设计摘要或 GEOMETRY_GAP、GEOMETRY_OVERLAP、REBAR_KEY_CONFLICT、SPAN_RECIPE_MISMATCH、STALE_RULE_DIGEST 等稳定错误。
2. POST /v1/joints/{id}/surface-evidence、/material-preparations、/mix-runs、/flow-tests：依次提交基面、原子领料租约、拌合和流动度数据；变更请求必须携带 operation_id、逻辑时间和预期聚合版本。
3. POST /v1/joints/{id}/fills、/curing-samples、/inspections：追加浇筑格、振实、养护及试件/拉拔/收缩/外观证据；响应包含当前连续前缀、证据判定和有序阻塞原因。
4. POST /v1/device-calls/{key}/retry 与 POST /v1/device-calls/{key}/result：按脚本化故障模型重试或提交设备结果，拒绝超次数、乱序、重复消费及格式错误回执。
5. POST /v1/joints/{id}/retests 与 POST /v1/retests/{id}/activate-generation：生成确定复验范围并激活处置代次；GET /v1/retests/{id} 返回有序谱系和完成条件。
6. POST /v1/joints/{id}/reviews 与 POST /v1/joints/{id}/verdicts：提交独立复核及通车、隔离或取消裁定；GET /v1/joints/{id} 和 /evidence 返回快照、材料谱系、前缀、养护时间轴、复验及终局。所有 JSON 列表使用固定领域排序，HTTP 状态与错误码映射固定。

## 失败边界

1. 单个写请求在数据库事务内同时校验聚合版本、幂等摘要、材料账、租约和领域状态；提交失败或进程中断时全部回滚，事件与快照必须同事务写入。
2. 材料领用和设备租约是一个原子边界；并发竞争由唯一约束和条件更新裁决，失败方不得获得部分材料、部分租约或可用盘次。
3. 填充写入以接缝版本和下一前缀坐标为条件更新；并发、越序、期限耗尽或租约失效均不得产生填充格、前缀推进或后续有效养护证据。
4. 外部仪器调用与业务事务分离，通过持久化待重试记录承接拒绝、断连和超时；只有符合调用键、尝试序号、代次及格式的成功回执可在新事务中形成读数。
5. 复验代次是证据隔离边界；旧代次永不删除，但激活新代次后其迟到结果不能满足新代次的检测或放行条件。
6. 终局表对接缝设置唯一记录并执行比较后写入；通车凭据与终局同事务产生，任意并发失败方不得遗留凭据。恢复时若事件校验或快照摘要不一致，服务进入只读故障状态并返回 RECOVERY_INTEGRITY_FAILED。

## 验收标准

1. 锁定操作完整固定槽口、钢筋、剪力键、基面测区、配方和批次、盘次与浇筑顺序、养护、试件、测点、邻接关系及阈值；错配或陈旧摘要被稳定拒绝。
2. 整数毫米几何校验可确定识别连续覆盖、缺口、交叠、退化区间、负值、钢筋与剪力键冲突和乘加溢出，并按规定领域键排序原因。
3. 整数克材料账在成功、并发竞争和失败回滚后始终守恒；材料扣减与全套设备租约原子完成，同操作号同内容幂等、不同内容冲突。
4. 基面、拌合、流动度、浇筑、振实和养护严格依赖推进；有效填充仅形成连续前缀，能拒绝跳段、重复层、错盘、期限边界、过期租约和逻辑时间倒退。
5. 全部配方及质量指标以确定整数定点计算，公开验证舍入、除零、负值和溢出；仪器故障只形成可恢复的有限次待重试调用，不产生伪造证据。
6. 异常依据邻接、同盘和共享测区生成唯一有序复验闭包；新代次完整保留材料与检测谱系，旧代次迟到回执不能改变当前结论。
7. 只有全部前置质量条件满足且两名不同合格人员独立复核后才能产生唯一通车凭据；通车、隔离和取消并发时恰有一个终局成功。
8. 服务使用真实持久化存储并可在重启后恢复材料余额、租约、代次、填充前缀、待重试调用、复验和终局；项目具备 go.mod、双架构 Docker 构建，生产代码与公开测试达到规定规模且不依赖真实时间、网络或随机顺序。

## 确定性测试场景

1. geometry_test.go：固定槽口坐标分别验证连续覆盖成功、缺口与交叠的确定排序、退化/负值拒绝，以及钢筋保护层与剪力键冲突。
2. geometry_test.go：构造接近 int64 边界的坐标尺度换算，断言 GEOMETRY_OVERFLOW 且任务未锁定。
3. material_lease_test.go：两个 goroutine 经同步屏障竞争同一材料和设备，仅一个事务成功，余额守恒且失败方无租约。
4. material_lease_test.go：模拟取得部分资源后的数据库提交失败，断言材料扣减、全部租约和盘次代次均回滚。
5. material_lease_test.go：相同 operation_id 同内容重试返回同一结果，不同称量内容返回 IDEMPOTENCY_CONFLICT。
6. workflow_test.go：以固定逻辑时间完成基面、拌合、流动度及顺序填充，逐步断言连续前缀坐标。
7. workflow_test.go：分别尝试跳段、重复层、错误盘次和逻辑时间倒退，断言稳定错误且前缀不变。
8. workflow_test.go：在施工截止前一刻写入成功，在等于截止时刻写入返回 MATERIAL_EXPIRED；过期租约同样不得支持写入。
9. fixedpoint_device_test.go：验证水胶比、纤维掺量、收缩率、强度与粘结强度的半远离零舍入，并覆盖除零、非法符号和乘法溢出。
10. fixedpoint_device_test.go：脚本化电子秤拒绝、流动度采集器超时和压力机格式错误，断言只形成有逻辑时间及确定次数的待重试记录。
11. retest_generation_test.go：浇筑中断通过相邻段与同盘关系传播，界面脱粘再经共享基面测区传播，断言唯一复验集合及完整稳定顺序。
12. retest_generation_test.go：激活新代次后提交旧代次迟到强度回执，断言仅归档且当前复验仍未完成。
13. verdict_recovery_test.go：缺少养护闭合、强度、拉拔或第二复核时分别拒绝通车；两名不同合格人员齐备后生成唯一凭据。
14. verdict_recovery_test.go：三个 goroutine 经同步屏障并发提交通车、质量隔离和取消，断言恰有一个终局且最多一个通车凭据。
15. verdict_recovery_test.go：在关键阶段关闭并重启服务，断言材料余额、填充前缀、设备租约、待重试调用、复验代次、幂等响应和终局均确定恢复。

## 组件追踪关系

1. 接缝设计与 UHPC 材料规则目录 → 锁定、整数毫米覆盖、冲突与定点规则 → acceptance 1、2、5 → geometry_test.go、fixedpoint_device_test.go。
2. 湿接缝任务及覆盖聚合 → 依赖状态机、只追加填充格、连续前缀和养护时间轴 → acceptance 4、8 → workflow_test.go、verdict_recovery_test.go。
3. 材料守恒与设备租约管理器 → 整数克守恒、并发原子领用、限时互斥租约与幂等 → acceptance 3、4、8 → material_lease_test.go、workflow_test.go。
4. 拌合浇筑及养护证据记录器 → 材料代次、施工期限、设备失败重试和施工证据 → acceptance 4、5、8 → workflow_test.go、fixedpoint_device_test.go。
5. 强度粘结复验与终局仲裁器 → 复验闭包、代次隔离、双人复核、单写终局 → acceptance 6、7、8 → retest_generation_test.go、verdict_recovery_test.go。
6. Go HTTP API → 稳定错误、固定排序、事务请求、持久化恢复与容器运行 → 全部 acceptance → 各测试文件通过 httptest 驱动同一公开接口。

## 独特性

本计划专注于装配式桥梁预制梁板之间 UHPC 湿接缝的连续浇筑前缀、盘次材料代次、界面拉拔与施工车辆荷载放行，区别于既有预制梁蒸养出池、墙板套筒灌浆支撑卸除等项目；其核心矛盾是湿接缝材料施工期限内的连续填充、跨分段复验传播及唯一通车终局。
